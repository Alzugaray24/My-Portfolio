import Form from "./Form";

function SignUp() {
  const containerStyle = {
    marginTop: "50px",
  };

  return (
    <div style={containerStyle}>
      <Form />
    </div>
  );
}

export default SignUp;